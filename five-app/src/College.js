import React, { Component } from 'react'

export default class College extends Component {
    render() {
        return (
            <h1 className={this.props.color}>Hello user</h1>
        )
    }
}
